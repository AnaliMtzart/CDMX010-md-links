{
  "name": "md-links",
  "version": "0.1.0",
  "scripts": {
    "test": "jest --watchAll",
    "test:coverage": "jest --coverage"
    
  },
  "dependencies": {
    "chalk": "^4.1.0",
    "jest": "^26.6.3",
    "marked": "^2.0.1",
    "node-fetch": "^2.6.1"
  }
}
